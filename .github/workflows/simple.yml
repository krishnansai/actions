name: linux commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: echo "sai welcome to actions"
      - name : list
        run: |
          systemctl start apache2.service
          curl ifconfig.me
          